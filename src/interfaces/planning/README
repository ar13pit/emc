Procedure to generate the setpoint from Corridor class:

1. Take input of four coordinates from perception block:
	a. Left Wall coordinate 1  : (x1, y1)
	b. Left Wall coordinate 2  : (x2, y2)
	c. Right Wall coordinate 1 : (x3, y3)
	d. Right Wall coordinate 2 : (x4, y4)

2. The SETPOINT_DISTANCE needs to be set in "config.h", also definition of PI

3. Include the necessary header file -> "planning.h" in your code

4. Copy the snippet given below to your code to get the setpoint:

   PointCorridor left1(x1, y1), left2(x2, y2), right1(x3, y3), right2(x4, y4);
   LineCorridor leftLine(left1, left2), rightLine(right1, right2);
   Corridor corridor(leftLine, rightLine);
   double destination_x, destination_y, destination_yangle;

   destination_x = corridor.get_corridor_setpoint().get_x();
   destination_y = corridor.get_corridor_setpoint().get_y();
   destination_angle = corridor.get_corridor_setpoint().get_angle();
   